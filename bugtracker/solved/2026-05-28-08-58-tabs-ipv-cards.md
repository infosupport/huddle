# Tabs ipv cards

**URL**: http://localhost:3000/#/container/devcontainer-empty1
**Datum**: 28-5-2026, 08:58:41

maak dit overview in tabs bovenaan te wisselen met eerst firewall dan docker dan noot. voeg dan ook nog een terminal window toe die connecteert naar de devcontainer en een cli opent die dan naar de webinterface wordt getrokken. maak voor dit laatste eerst een plan.

## Implementatie tabs — 28-5

`container-detail.component.{ts,html}` herschreven:

- **Altijd zichtbaar bovenaan**: container-naam + snapshot-knop, en een info-strip (Image, Aangemaakt, Workspace, Bronmap, IP, Netwerk-status, Huddle ↔ dc-net status). De info-strip bleef expres buiten de tabs omdat hij contextueel relevant is voor elke tab.
- **Tab-strip** met vier tabs in deze volgorde: Firewall, Docker, Noot, Terminal. State in `activeTab: 'firewall' | 'docker' | 'noot' | 'terminal'`. Klikbaar via `setTab()`.
- **Firewall** (default-tab): de twee bestaande rule-secties (container-specifiek + globaal), met allow / temp-allow / deny / requested tabellen.
- **Docker**: het bestaande Docker-access-grant blok (timer + +5/+10/+15/+30 knoppen).
- **Noot**: de bestaande gebruiker-uitleg, wachtwoord-veld + kopieerknop, en het su-commando-codeblok.
- **Terminal**: placeholder met verwijzing naar onderstaand plan.

CSS: `.tabs` / `.tabs .tab` toegevoegd in `src/styles.css` (border-bottom strip, active state met accent-kleur).

## Plan: terminal-tab embedden

Doel: in de Terminal-tab een live terminal die een shell opent in de geselecteerde devcontainer (zoals `docker exec -it <name> bash`), volledig binnen het portaal — dus geen lokale terminal nodig.

### Architectuur

1. **Frontend** (Terminal-tab):
   - [xterm.js](https://xtermjs.org/) als render-laag (canvas-based, kleine bundle, ondersteunt resize/copy/paste/colors).
   - WebSocket-verbinding naar een nieuwe huddle-endpoint die rauwe stdin/stdout bytes shuttled.
   - Auto-fit add-on (`@xterm/addon-fit`) zodat de terminal de container-grootte volgt; resize event geeft `{cols, rows}` door over de socket.

2. **Backend** (`gateway/src/`):
   - Nieuwe WS-route `GET /ws/exec/:container` (upgrade-handler via de bestaande `wss`).
   - Auth: dezelfde source-IP check (`isDevcontainerSource`) — devcontainer-netwerken mogen géén exec doen op andere containers. Alleen requests via de host-port `:3000` (admin) zijn toegestaan.
   - Per WS-verbinding één Docker exec-instance:
     1. `POST /containers/:id/exec` met `{Cmd: ["/bin/bash", "-l"], AttachStdin: true, AttachStdout: true, AttachStderr: true, Tty: true, User: "vscode"}` → krijgt `execId`.
     2. `POST /exec/:execId/start` met `{Detach: false, Tty: true}` — geeft een hijacked HTTP-stream terug die bidirectioneel bytes voert (Docker stream-format met TTY=true is rauwe pty, geen multiplexed header).
     3. WS → Docker: alle binary frames doorpipen naar de exec-stream.
     4. Docker → WS: alles wat uit de stream komt direct als binary frame naar WS.
   - Resize: aparte tekst-frame `{"type":"resize","cols":N,"rows":M}` triggert `POST /exec/:execId/resize?h=M&w=N`.
   - Cleanup: WS-close → exec-stream destroy. Een exec zonder open stdin eindigt vanzelf wanneer de shell exit; Docker ruimt de pty op.

3. **Security**:
   - Geen pad-traversal mogelijk: container-naam moet matchen op naam uit `listDevcontainers()` whitelist.
   - Audit: log start/stop van elke exec via `logAudit({container, domain: 'terminal', action: 'open'/'close'})`. Sudo-commando's binnen die shell vallen al onder het bestaande sudo-audit forwarder script in elke container.
   - Default-user `vscode`. `noot` is bereikbaar via `su - noot` in de shell zelf, niet als WS-parameter (anders kan de UI auth bypassen).

4. **Frontend-tab gedrag**:
   - WS pas openen wanneer de Terminal-tab geactiveerd wordt (lazy mount).
   - Bij tab-switch weg: WS open laten (om sessie te behouden tijdens kort weg-klikken naar Firewall), maar `dispose()` op component-destroy.
   - Indicator boven de terminal: "Verbonden als vscode" / "Verbinding verloren — opnieuw verbinden" knop bij socket-close.

### Gefaseerde uitrol

- **Fase 1** — WS-tunnel + xterm.js basic: open shell, lees/schrijf bytes, geen resize, geen reconnect. Genoeg om iets werkends te zien.
- **Fase 2** — resize support, copy/paste, kleurthema-koppeling.
- **Fase 3** — sessie-persistentie (zelfde execId hergebruiken bij reconnect zolang de pty leeft), audit-log integratie, "switch to noot" sneltoets die `su - noot` voorvult.

### Open vragen voor jou

- Wil je dat de shell als `vscode` óf direct als `noot` opent? Default `vscode` houden lijkt me veilig (sudo blijft expliciet via `su - noot`).
- Mag de terminal-tab ook werken vanuit een non-admin browser-tab (Same-Origin sufficient), of wil je een extra MFA/grant-stap voor exec zoals het bestaande `setGrant` model voor docker.sock?
- Bundle-grootte: xterm.js is ~150kB gzipped. OK of te zwaar voor de gateway-image?

## Fase 1+2 geïmplementeerd — 29-5

**Backend** — nieuwe `gateway/src/terminal.ts`:
- `attachTerminal(ws, containerName)` valideert de naam tegen `listDevcontainers()` (whitelist), opent Docker `POST /containers/<name>/exec` met `Cmd=["/bin/bash","-l"]`, `Tty=true`, `User=vscode`, `Env=[TERM=xterm-256color]`, dan `POST /exec/<id>/start` met `Detach=false, Tty=true` + `Upgrade: tcp` headers zodat Docker de socket hijack't en we rauwe bidirectional pty-bytes krijgen (geen multiplex-header bij TTY=true).
- WS→Docker pipet binary frames (en non-JSON text-frames) als keystrokes; tekst-frames met `{"type":"resize","cols":N,"rows":M}` triggeren `POST /exec/<id>/resize?h=M&w=N`.
- `logAudit({domain:'terminal', action:'open'|'close:<reason>'})` bij open en close.

**Backend** — `gateway/src/api.ts`:
- Tweede `WebSocketServer` `wssTerminal` (noServer) puur voor terminal-flows. Upgrade-handler dispatcht `/ws/exec/<container>` daarheen, het bestaande `/ws` blijft state-push. Devcontainer-source-IP check zit al in de globale upgrade-handler, dus dit endpoint is automatisch admin-only.

**Frontend** — nieuwe `gateway/frontend/src/app/shared/components/container-terminal/`:
- Standalone component dat xterm.js + `@xterm/addon-fit` opzet in `AfterViewInit`, WS opent naar `/ws/exec/<name>` (binary=ArrayBuffer), pipet keystrokes via `term.onData`, schrijft binnenkomende bytes via `term.write`.
- `ResizeObserver` op de host-div triggert `fit.fit()` + JSON-resize-frame over de WS bij elke layout-wijziging.
- Statusbar boven het venster ("Verbonden" / "Verbinding gesloten (code)") + "Opnieuw verbinden" knop voor manual reconnect. Bij component-destroy worden WS + xterm netjes gedisposed.

**Frontend** — `container-detail.component.{ts,html}`:
- `<app-container-terminal [container]="name"/>` vervangt de placeholder. Component is een import in `imports[]` van het host-component. Mount gebeurt pas wanneer de Terminal-tab actief is (`@if (activeTab === 'terminal')`), dus geen onnodige WS-verbindingen.

**Deps** — `gateway/frontend/package.json`:
- `@xterm/xterm ^5.5.0` + `@xterm/addon-fit ^0.10.0` (pure JS, geen native deps).

### Niet geïmplementeerd (uit fase 3)

- Sessie-persistentie over WS-reconnect (zelfde execId herbenutten zolang pty leeft) — bij reconnect krijg je een verse shell.
- "Switch to noot" sneltoets die `su - noot` voorvult in de prompt.
- Copy/paste werkt via xterm's defaults (Ctrl+Shift+C/V op Linux, Cmd+C/V op Mac); geen custom binding bovenop.

### Open punt voor jou

Resize works via `term.cols` × `term.rows`, dus de container-shell vult de viewport mee. Als je de tab klein krijgt is `fit` luidruchtig kort daarna; voor smoothness kan een `requestAnimationFrame` debounce nuttig zijn — laat het me weten als je dat tegenkomt.
